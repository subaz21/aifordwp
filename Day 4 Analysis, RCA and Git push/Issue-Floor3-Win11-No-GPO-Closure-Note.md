Resolved. Cause: Floor 3 DHCP scope still pointed clients to a decommissioned DNS server, which blocked domain controller resolution and Group Policy retrieval at startup. Action: confirmed DNS/DC/GPO failure chain in events, corrected DHCP DNS option to 10.10.0.10, renewed affected client leases, and validated successful Group Policy processing. Preventive: enforce DHCP scope validation during DNS migrations, add subnet endpoint sampling, and implement DNS option drift plus GPO failure correlation alerting. User confirmed working.
