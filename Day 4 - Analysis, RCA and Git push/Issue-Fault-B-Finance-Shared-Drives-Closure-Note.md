Resolved. Cause: drive mapping script migration from user logon to SYSTEM context caused UNC mapping failure before required user-context/network dependencies were available. Action: confirmed SYSTEM-context script failure pattern, moved mapping back to user-context execution, added readiness and retry controls, and validated restored access to the Finance shared drive. Preventive: enforce context validation, startup dependency checks, and canary rollout gates for endpoint script migrations. User confirmed working.
